/* Copy the previous code and use it as a base.
After the original code, add code to change the third element to “gray”.

Print the third element again after the change.

Run your program to check that it works. */

const arr = ['the', 'quick', 'brown', 'fox'];
console.log(arr);

arr[2] = "gray";
console.log(arr[2]);
