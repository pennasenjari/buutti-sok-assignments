Assignment 16.9: Art Gallery with React

Create a new React App, and remove the unnecessary parts, like you did in the previous exercise.

Create three new components, name each of them however you like. Each component should
 include an image and a title. Add in CSS styling to style the borders of the image.

Import all these components to App.jsx and:

a) render all three of them in single row.

b) render only two of them in single column.

c) extra render all of them in single row but first image has been rotated 45 degrees
 to the left, second 90 degrees to the left with borders and third one 180 degrees.

d) extra Make the third image spin around by using CSS.