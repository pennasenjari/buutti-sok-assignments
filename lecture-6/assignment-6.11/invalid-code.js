/* The following code doesn't work. 
Figure out why it doesn't work, and fix it so that it works.

const value = 30;
const multiplier = 1.2;
value = value * multiplier;
console.log(value);
 */

// Variable "value" cannot be changed because it is a constant. Define with let.
let value = 30;
const multiplier = 1.2;
value = value * multiplier;
console.log(value);
